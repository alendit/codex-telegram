# Agent Workspace Template

This directory is an example `/agent` workspace for the `codex app-server`
runtime used by `codex-telegram`.

Copy and adapt it in your private deployment if you want the runtime container
to start with project instructions, Codex config defaults, or helper skills.
